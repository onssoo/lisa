# Public review copy

This is a **sanitized public copy** of a private repository, published so that an external
reviewer can read the design of lisa.

Changed for publication: internal network addresses → placeholders; personal names, a street
address and a doctor's name → generic placeholders; references to private repositories → their
public upstreams; the git history is regenerated as a single commit (the private repository
keeps the full history).

No collected personal data is in this repository, and never was: it holds the design and the
review only — the running system keeps all captured data on the host machine.

Contents: everything under `docs/` (except `docs/archive/`), plus this notice.
Generated from the private source of truth by an export script; do not edit this copy directly.
